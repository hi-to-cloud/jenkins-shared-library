
  pipeline {
      agent any
      stages {
          stage('Hello') {
              steps {
                  def helloScript = load("./vars/hello.groovy")
                  helloScript.call()
              }
          }
  }
}
